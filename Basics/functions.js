function calPrice(num1) {
    return num1;

}

const user = {
    name : "Showvik",
    age : 23,
    city : "Cox's Bazar"
}

function findOut(anyobject){
    console.log(`This is the name ${anyobject.name} and this is the age ${anyobject.age}`)
}

console.log(calPrice(2));

findOut(user);

